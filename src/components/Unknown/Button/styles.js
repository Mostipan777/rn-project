import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonTitle: {
    fontSize: 18,
    fontWeight: '700',
    color: 'white',
  },
  button: {
    alignItems: 'center',
    borderRadius: 10,
    marginHorizontal: 24,
    paddingVertical: 22,
  },
});
